#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    double a = 5.6;
    double b = 3.2;

    // printf("%f %f", floor(a), round(b)); 5 3
    // printf("%f %f", round(a), floor(b)); 6 3
    // printf("%f %f", ceil(a), ceil(b)); 6 4
    return 0;
}